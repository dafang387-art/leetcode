知识点：
（1）Vector的相关操作：
    初始化：vector<int> name(num,value),创建一个存储int类型的向量，其中包括num个value值
    初始化2：vector<int> name = {1,2,3,4,5}
    拷贝：vector<int> name(other_name
    访问：v[i]
    尾插/尾删：v.push_back(value)/v.pop_back()
    判断空值：v.empty()
    获取首尾引用: v.back()
    如果想改变一个向量的大小：两种方式：v.revise(n)是改变当前的大小为n，v.reserve(n)是预留空间，只分配内存不创建对象

二分查找：
class Solution {
public:
    int search(vector<int>& nums, int target) {
       int left = 0;
       int right = nums.size()-1;
       while(left<=right){
         // 避免超出int范围
         int mid = left + (right-left)/2;
         int num = nums[mid];
            if(num==target){
                return mid;
            }else if(num<target){
                left=mid+1;
            }else if(num>target){
                right=mid-1;
            }
       }
       return -1;
    }
};
    
    
